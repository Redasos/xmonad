              https://github.com/Redasos/xmonad

1. su

---------------------------------------------------------------------------------------------------------------------------------

2. nano /etc/modules-load.d/zram.conf

zram

--------------------------------------------------------------------------------------------------------------------------

3. nano /etc/modprobe.d/zram.conf

options zram num_devices=2   

------------------------------------------------------------------------------------------------------------------------------------------------

4. nano /etc/udev/rules.d/99-zram.rules     

KERNEL=="zram0", ATTR{disksize}="2048Mb" RUN="/usr/bin/mkswap /dev/zram0", TAG+="systemd"
KERNEL=="zram1", ATTR{disksize}="2048Mb" RUN="/usr/bin/mkswap /dev/zram1", TAG+="systemd"

-----------------------------------------------------------------------------------------------------------------------------------

5. nano /etc/fstab

/dev/zram0 none swap defaults 0 0
/dev/zram1 none swap defaults 0 0

----------------------------------------------------------------------------------------------------------------

6. echo "vm.swappiness=15" > /etc/sysctl.d/swap.conf    

-------------------------------------------------------------------------------------------
-------------------------------------------
--------------------------
editor=vim;nano;geany;pluma;gedit;mousepad;atom and ...........! 
